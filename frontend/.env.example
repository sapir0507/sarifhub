# Copy to .env.local to override. No secrets belong in frontend env files: everything here ends up in the bundle.
# "hash" = hash-based routing for static hosting without server fallback. Leave empty for normal routing.
VITE_ROUTER=
